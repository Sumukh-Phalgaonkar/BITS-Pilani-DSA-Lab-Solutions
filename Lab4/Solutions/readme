myalloc and myfree functions are implemented within linkedlist.h
int testCyclic(struct linkedlist* ls) returns 1 if the passed linkedlist is cyclic, and returns 0 if the passed linkedlist is linear. 
